; Pytest runs the suite under tests/ only. The root test_*.py files are the
; legacy standalone scripts (each has main() -> int, exit 0 = pass); they are
; executed one-per-test by tests/test_legacy_suite.py, so pytest must not try
; to collect them directly.
[pytest]
testpaths = tests
addopts = -q
